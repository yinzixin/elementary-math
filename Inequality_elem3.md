# 平方平均与算术平均——$Q \geq A$ 的变形与证明



---

## 一、四种平均数的位置

对两个正数 $a, b > 0$，我们已经认识了四种平均数：

$$H = \frac{2ab}{a+b}, \quad G = \sqrt{ab}, \quad A = \frac{a+b}{2}, \quad Q = \sqrt{\frac{a^2+b^2}{2}}$$

前两篇文章分别证明了 $A \geq G$ 和 $G \geq H$。这篇文章的主角是最靠右的不等式：

$$\boxed{Q \geq A}$$

即**平方平均（均方根）$\geq$ 算术平均**。

四种平均数构成完整的不等式链：

$$H \leq G \leq A \leq Q$$

等号当且仅当 $a = b$。

---

## 二、先看几个例子

| $a$ | $b$ | $A = \dfrac{a+b}{2}$ | $Q = \sqrt{\dfrac{a^2+b^2}{2}}$ | 谁大？ |
|-----|-----|----------------------|---------------------------------|--------|
| 1 | 9 | 5 | $\sqrt{41} \approx 6.40$ | $Q$ 大 |
| 4 | 4 | 4 | $\sqrt{16} = 4$ | **相等** |
| 2 | 8 | 5 | $\sqrt{34} \approx 5.83$ | $Q$ 大 |
| 3 | 5 | 4 | $\sqrt{17} \approx 4.12$ | $Q$ 大 |

规律清楚：$Q \geq A$，只有 $a = b$ 时取等。

---

## 三、主定理

**定理**（平方平均 $\geq$ 算术平均）

对任意正数 $a, b > 0$，有

$$\sqrt{\frac{a^2+b^2}{2}} \geq \frac{a+b}{2}$$

等号成立当且仅当 $a = b$。

### 证明

两边均为正数，同时平方，不等号方向不变：

$$\frac{a^2+b^2}{2} \geq \left(\frac{a+b}{2}\right)^2 = \frac{(a+b)^2}{4}$$

两边乘以 $4$：

$$2(a^2+b^2) \geq (a+b)^2$$

展开右边：

$$2a^2+2b^2 \geq a^2+2ab+b^2$$

移项整理：

$$a^2-2ab+b^2 \geq 0$$

$$(a-b)^2 \geq 0 \quad \checkmark$$

等号成立当且仅当 $a = b$。$\blacksquare$

---

## 四、常见变形

### 变形 1：展开形式

由 $Q \geq A$ 两边平方，可得等价的展开不等式：

$$2(a^2+b^2) \geq (a+b)^2$$

**题目**：证明对任意正数 $a, b$，有 $2(a^2+b^2) \geq (a+b)^2$，并指出等号成立的条件。

**证明**：

$$2(a^2+b^2) - (a+b)^2 = 2a^2+2b^2-a^2-2ab-b^2 = a^2-2ab+b^2 = (a-b)^2 \geq 0 \quad \checkmark$$

等号当且仅当 $a = b$。$\blacksquare$

> **与第一篇的联系**：将上式移项得 $a^2+b^2 \geq 2ab$，正是 $A \geq G$ 的平方形式。这说明 $Q \geq A$ 蕴含了 $A \geq G$，$Q \geq A$ 是更强的不等式。

---

### 变形 2：已知和，推出平方和的下界

由 $2(a^2+b^2) \geq (a+b)^2$，两边除以 $2$：

$$a^2+b^2 \geq \frac{(a+b)^2}{2}$$

**题目**：已知正数 $a, b$ 满足 $a+b = 6$，证明 $a^2+b^2 \geq 18$，并指出等号成立的条件。

**证明**：

$$a^2+b^2 \geq \frac{(a+b)^2}{2} = \frac{36}{2} = 18 \quad \checkmark$$

等号当且仅当 $a = b = 3$。$\blacksquare$

> 口诀：**和固定时，平方和最小在两数相等**。

---

### 变形 3：已知平方和，推出和的上界

由 $2(a^2+b^2) \geq (a+b)^2$，改写方向：

$$(a+b)^2 \leq 2(a^2+b^2)$$

两边取平方根（两边均非负，方向不变）：

$$a+b \leq \sqrt{2(a^2+b^2)}$$

**题目**：已知正数 $a, b$ 满足 $a^2+b^2 = 18$，证明 $a+b \leq 6$，并指出等号成立的条件。

**证明**：

$$(a+b)^2 \leq 2(a^2+b^2) = 36$$

故 $a+b \leq 6$。等号当且仅当 $a = b = 3$。$\blacksquare$

> 口诀：**平方和固定时，和最大在两数相等**。

---

### 变形 4：令 $b = ka$（比例代换）

在 $2(a^2+b^2) \geq (a+b)^2$ 中令 $b = ka$（$k > 0$）：

$$2a^2(1+k^2) \geq a^2(1+k)^2$$

两边除以 $a^2 > 0$：

$$2(1+k^2) \geq (1+k)^2$$

**题目**：证明对任意正数 $k$，有 $2(1+k^2) \geq (1+k)^2$，并指出等号成立的条件。

**证明**：

$$2(1+k^2) - (1+k)^2 = 2+2k^2-1-2k-k^2 = k^2-2k+1 = (k-1)^2 \geq 0 \quad \checkmark$$

等号当且仅当 $k = 1$，即 $b = a$。$\blacksquare$

> 这说明 $Q \geq A$ 的本质只依赖于两数的**比值** $k = b/a$，与具体数值的大小无关。

---

### 变形 5：完整不等式链的验证

综合前三篇的结论：

$$\underbrace{\frac{2ab}{a+b}}_{H} \leq \underbrace{\sqrt{ab}}_{G} \leq \underbrace{\frac{a+b}{2}}_{A} \leq \underbrace{\sqrt{\frac{a^2+b^2}{2}}}_{Q}$$

**题目**：已知 $a = 1, b = 9$，计算 $H, G, A, Q$，验证不等式链成立。

**解**：

$$H = \frac{2 \times 1 \times 9}{1+9} = \frac{18}{10} = 1.8, \quad G = \sqrt{9} = 3, \quad A = \frac{10}{2} = 5, \quad Q = \sqrt{\frac{1+81}{2}} = \sqrt{41} \approx 6.40$$

确实 $1.8 < 3 < 5 < 6.40$。$\blacksquare$

---

### 变形 6：两边取平方后再与 $G$ 比较

由变形 1，$2(a^2+b^2) \geq (a+b)^2$，而由第一篇知 $(a+b)^2 \geq 4ab$，故：

$$2(a^2+b^2) \geq (a+b)^2 \geq 4ab$$

即 $a^2+b^2 \geq 2ab$（左侧不等式）和 $(a+b)^2 \geq 4ab$（右侧不等式）同时成立。

**题目**：证明 $\dfrac{a^2+b^2}{2} \geq ab$（$a, b > 0$），即 $Q^2 \geq G^2$，从而 $Q \geq G$。

**证明**：

$$\frac{a^2+b^2}{2} - ab = \frac{a^2-2ab+b^2}{2} = \frac{(a-b)^2}{2} \geq 0 \quad \checkmark$$

等号当且仅当 $a = b$。$\blacksquare$

> 这给出了 $Q \geq G$ 的直接证明，无需经过 $A$。

---

## 五、用 $Q \geq A$ 求最值

均值不等式最强大的用途：已知平方和求和的最大，已知和求平方和的最小。

### 例题 1：已知平方和，求和的最大值

**题目**：$a^2+b^2 = 50$，$a, b > 0$，求 $a+b$ 的最大值。

**解**：由变形 3：

$$(a+b)^2 \leq 2(a^2+b^2) = 100$$

故 $a+b \leq 10$。等号当 $a = b = 5$ 时取到，最大值为 $\boxed{10}$。

---

### 例题 2：已知和，求平方和的最小值

**题目**：$a+b = 8$，$a, b > 0$，求 $a^2+b^2$ 的最小值。

**解**：由变形 2：

$$a^2+b^2 \geq \frac{(a+b)^2}{2} = \frac{64}{2} = 32$$

等号当 $a = b = 4$ 时取到，最小值为 $\boxed{32}$。

---

### 例题 3：直接求 $Q$ 的最小值

**题目**：$a+b = 10$，$a, b > 0$，求 $Q = \sqrt{\dfrac{a^2+b^2}{2}}$ 的最小值。

**解**：由 $Q \geq A = \dfrac{a+b}{2} = 5$，

等号当 $a = b = 5$ 时取到，$Q$ 的最小值为 $\boxed{5}$。

---

### 例题 4：矩形的平方和与对角线

**题目**：用 $20$ 米篱笆围一个矩形，设两边长为 $a$ 和 $b$（单位：米），则 $a+b = 10$。

1. 求面积 $S = ab$ 的最大值。（用 $A \geq G$）
2. 求对角线长度 $d = \sqrt{a^2+b^2}$ 的最小值。（用 $Q \geq A$）

**解**：

**第 1 问**：由 $A \geq G$：$\dfrac{a+b}{2} \geq \sqrt{ab}$，即 $5 \geq \sqrt{ab}$，故 $ab \leq 25$。

等号当 $a = b = 5$（正方形）时取到，最大面积为 $25$ 平方米。

**第 2 问**：由变形 2：$a^2+b^2 \geq \dfrac{(a+b)^2}{2} = 50$，

故 $d = \sqrt{a^2+b^2} \geq \sqrt{50} = 5\sqrt{2}$。

等号当 $a = b = 5$ 时取到，最短对角线长度为 $5\sqrt{2} \approx 7.07$ 米。

> **有趣的发现**：面积最大和对角线最短，同时在正方形时取到！

---

## 六、常见错误提醒

| 错误做法 | 正确理解 |
|----------|----------|
| 平方时不检查两边是否非负 | 只有两边都**非负**时，平方才保持不等号方向 |
| 将 $Q \geq A$ 与 $A \geq G$ 弄反 | $Q \geq A \geq G \geq H$，$Q$ 是四种平均中**最大**的 |
| 把 $Q$ 误写为 $\sqrt{a^2+b^2}$ | 注意 $Q = \sqrt{\dfrac{a^2+b^2}{2}}$，分母是 $\mathbf{2}$，别漏掉 |
| 忘记验证等号能取到 | 求最值时必须验证等号成立的条件 |

---

## 七、总结

$$Q = \sqrt{\dfrac{a^2+b^2}{2}} \geq A = \dfrac{a+b}{2} \qquad (a,b>0)$$

| 变形 | 结论 | 等号条件 |
|------|------|----------|
| 原式 | $\sqrt{\dfrac{a^2+b^2}{2}} \geq \dfrac{a+b}{2}$ | $a=b$ |
| 两边平方 | $2(a^2+b^2) \geq (a+b)^2$ | $a=b$ |
| 已知和 $s$ | $a^2+b^2 \geq \dfrac{s^2}{2}$（平方和的下界） | $a=b$ |
| 已知平方和 $k$ | $a+b \leq \sqrt{2k}$（和的上界） | $a=b$ |
| 令 $b=ka$ | $2(1+k^2) \geq (1+k)^2$ | $k=1$ |
| 推论 | $\dfrac{a^2+b^2}{2} \geq ab$（即 $Q \geq G$） | $a=b$ |

完整不等式链：

$$\frac{2ab}{a+b} \leq \sqrt{ab} \leq \frac{a+b}{2} \leq \sqrt{\frac{a^2+b^2}{2}}$$

**一句话记住核心**：平方平均 $\geq$ 算术平均，等号当两数相等时成立，是四种平均中最大的。

---

## 八、练习题（无答案）

### A 组：仿照例题 1（已知平方和，求和的最大值）

**A1.** 已知正数 $a, b$ 满足 $a^2+b^2 = 8$，求 $a+b$ 的最大值，并指出取到最大值时 $a, b$ 各为多少。

**A2.** 已知正数 $a, b$ 满足 $a^2+b^2 = 72$，求 $a+b$ 的最大值，并指出取到最大值时 $a, b$ 各为多少。

**A3.** 已知正数 $a, b$ 满足 $a^2+b^2 = 2$，求 $Q = \sqrt{\dfrac{a^2+b^2}{2}}$ 的值，再求 $a+b$ 的最大值，并验证 $Q \geq A$。

---

### B 组：仿照例题 2（已知和，求平方和的最小值）

**B1.** 已知正数 $a, b$ 满足 $a+b = 4$，求 $a^2+b^2$ 的最小值，并指出取到最小值时 $a, b$ 各为多少。

**B2.** 已知正数 $a, b$ 满足 $a+b = 10$，求 $a^2+b^2$ 的最小值。

**B3.** 已知正数 $a, b$ 满足 $a+b = 6$，分别求 $a^2+b^2$ 的最小值以及 $Q = \sqrt{\dfrac{a^2+b^2}{2}}$ 的最小值。

---

### C 组：综合运用三种平均

**C1.** 已知正数 $a, b$ 满足 $a^2+b^2 = 50$，求 $ab$ 的最大值。（提示：$(a+b)^2 = a^2+2ab+b^2$，先求 $a+b$ 的最大值）

**C2.** 设 $a+b = 6$，$a, b > 0$，计算或估计 $H, G, A, Q$ 各自的范围，并用不等式链表示它们的大小关系（等号条件写清楚）。

**C3.** 证明：对任意正数 $a, b$，有 $\sqrt{\dfrac{a^2+b^2}{2}} \geq \sqrt{ab}$（即 $Q \geq G$），并说明这可以由 $Q \geq A \geq G$ 推出，也可以直接证明。

---

### D 组：仿照例题 4（几何与实际问题）

**D1.** 矩形的周长为 $2s$（即 $a+b = s$，$a, b > 0$）。
1. 求面积 $ab$ 的最大值。
2. 求对角线 $\sqrt{a^2+b^2}$ 的最小值，并说明取最小值时矩形是正方形。

**D2.** 小明用两段绳子，第一段长 $a$ 分米，第二段长 $b$ 分米，满足 $a^2+b^2 = 200$。
1. 求两段绳子总长 $a+b$ 的最大值。
2. 若将两段绳子分别围成正方形，两个正方形的面积之和是多少？面积之和与总长的关系符合哪个不等式？
