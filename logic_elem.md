# 逻辑学入门：读懂数学证明背后的"思维规则"

你刚学完不等式的证明，是不是常常有这种感觉：

> "老师写的每一步我都看得懂，但合起来为什么就能证明结论？"
> "我自己写的证明，老师为什么说'逻辑不通'？"

这其实不是数学问题，而是**逻辑**问题。这篇教程就来讲讲，证明背后的那套"游戏规则"。



## 一、什么是"命题"

**命题**就是一句"非真即假"的陈述句。

- "3 大于 2" —— 是命题（真）
- "5 是偶数" —— 是命题（假）
- "今天天气真好啊！" —— 不是命题（感叹，没法判断真假）
- "$x > 0$" —— 在 $x$ 没给定之前，是"开命题"，给了 $x$ 就能判定

**记住一件事**：数学证明里出现的每一句话，几乎都应当是命题。如果你写下一句既不是定义、也不是命题的话，逻辑就会断掉。

> **例题**：判断下面哪些是命题，是命题的请说出真假。
> 1. "7 是质数"
> 2. "请把窗户关上。"
> 3. "三角形有三条边"
> 4. "他跑得快吗？"
>
> **解**：1 是命题（真）；3 是命题（真）；2 是祈使句、4 是疑问句，都**没法判断真假**，所以都不是命题。

---

## 二、最基本的逻辑连接词

数学证明里反复出现下面几个词，看似熟悉，其实有严格含义：

### 1. "且"（并且，and）
"P 且 Q"为真 $\iff$ P 真 **并且** Q 也真。
例：$a > 0$ 且 $b > 0$，意思是两者**同时**成立。

### 2. "或"（or）
"P 或 Q"为真 $\iff$ P、Q **至少有一个**为真。
注意：数学里的"或"**不排斥两个都成立**。
例：$x \geq 0$ 其实是 "$x > 0$ 或 $x = 0$"。

### 3. "非"（否定，not）
"非 P" 把真假翻过来。
- "非（$x > 0$）" 不是 "$x < 0$"，而是 "$x \leq 0$"（包括等号！）
- 这是初中生最常犯的错误。

### 4. "如果……那么……"（蕴含，$\Rightarrow$）
写作 $P \Rightarrow Q$，读作 "P 推出 Q"。
意思是：**只要 P 成立，Q 一定成立**。

**关键**："$P \Rightarrow Q$" 为真，**不代表** "$Q \Rightarrow P$" 也为真。

> 例：$x > 2 \Rightarrow x > 0$（对的）
> 但 $x > 0 \Rightarrow x > 2$（错的，$x$ 可能是 1）

### 5. "当且仅当"（$\iff$）
$P \iff Q$ 意思是 P 和 Q **互相推出**：$P \Rightarrow Q$ 且 $Q \Rightarrow P$。
两个命题真假完全一致。

> **例题 1**：写出 "$x \geq 3$" 的否定。
> **解**："非（$x \geq 3$）" 就是 "$x < 3$"（注意不带等号，因为 $x = 3$ 本身是满足 $x \geq 3$ 的）。
>
> **例题 2**：下面两句哪句对？
> 1. $x = 2 \Rightarrow x^2 = 4$
> 2. $x^2 = 4 \Rightarrow x = 2$
>
> **解**：第 1 句对（2 的平方就是 4）；第 2 句**错**，因为 $x$ 也可能等于 $-2$。这说明 "$\Rightarrow$" 不能随便反过来用。

---

## 三、证明里最常用的两条逻辑规则

整个不等式证明，几乎都建立在下面两条规则上：

### 规则一：传递性（连环推理）

> 如果 $P \Rightarrow Q$，并且 $Q \Rightarrow R$，那么 $P \Rightarrow R$。

这就是为什么证明可以一行接一行地推下去：

$$
a > b \quad (\text{已知})
$$
$$
\Rightarrow a - b > 0
$$
$$
\Rightarrow (a-b)^2 \geq 0 \quad \cdots\cdots \text{等等}
$$

每一步都必须保证 **上一行 $\Rightarrow$ 这一行**。如果中间有一步推不出，整条链就断了。

### 规则二：分离规则（Modus Ponens）

> 已知 "P 成立"，又知 "$P \Rightarrow Q$"，就可以断定 "Q 成立"。

这是你引用定理时所用的规则。例如：

- 定理："对任意实数 $x$，$x^2 \geq 0$"（这是 "$P \Rightarrow Q$"）
- 你写下："因为 $(a-b)^2 \geq 0$"，就是在用 P = "$a-b$ 是实数" 推出 Q。

> **例题（传递性）**：已知 $a > 5$，$5 > b$，能得到什么结论？
> **解**：由 $a > 5$ 且 $5 > b$，根据传递性可得 $a > b$。这正是连环推理：$a > 5 \Rightarrow$（接上）$5 > b \Rightarrow a > b$。
>
> **例题（分离规则）**：已知定理"偶数加偶数还是偶数"，又知道 6 和 8 都是偶数，能断定什么？
> **解**：把 P = "6 和 8 都是偶数" 代入定理 $P \Rightarrow Q$，立刻断定 Q = "$6 + 8$ 是偶数"，即 14 是偶数。

---

## 四、用逻辑视角重看一道证明

**题目**：已知 $a, b > 0$，证明 $a + b \geq 2\sqrt{ab}$。

**证明**：

$$
\text{因为 } (\sqrt{a} - \sqrt{b})^2 \geq 0 \quad \cdots\cdots (1)
$$
$$
\text{展开：} a - 2\sqrt{ab} + b \geq 0 \quad \cdots\cdots (2)
$$
$$
\text{移项：} a + b \geq 2\sqrt{ab} \quad \cdots\cdots (3)
$$

我们用逻辑放大镜看每一步：

| 步骤 | 这是什么类型的句子？ | 为什么能这样写？ |
|---|---|---|
| (1) | 一个命题 | 引用定理："任何实数的平方非负" |
| $(1) \Rightarrow (2)$ | 蕴含 | 代数恒等变形（等价变换） |
| $(2) \Rightarrow (3)$ | 蕴含 | 不等式两边同时加 $2\sqrt{ab}$，方向不变 |
| 结论 | 命题 (3) 为真 | 由传递性：$(1) \Rightarrow (2) \Rightarrow (3)$ |

看出来了吗？**每一步都必须能写出"由什么推出"**。如果你写不出来，那一步就是"凭感觉"，老师就会说你逻辑不通。

---

## 五、初中生最常踩的 4 个逻辑坑

### 坑 1：把"$\Rightarrow$"当成"$\iff$"

写证明时用了"$\Rightarrow$"，但实际只是单向的，不能反过来用。

**错误示范**：
> 要证 $a^2 + b^2 \geq 2ab$。
> 因为 $a^2 + b^2 \geq 2ab$，所以 $(a-b)^2 \geq 0$，成立。

这是**从要证的结论出发**推出已知，方向反了！正确写法是从 $(a-b)^2 \geq 0$ 出发，推回结论；或者明确写出每一步都是 $\iff$（等价变形）。

### 坑 2：忽略条件

定理"$\sqrt{ab} \leq \dfrac{a+b}{2}$"的前提是 $a, b \geq 0$。你用的时候必须先验证条件成立，否则就是"无效引用"。

### 坑 3：用要证的东西去证它自己（循环论证）

> 要证 A。
> 因为 A，所以 B；又因为 B，所以 A。

这种证明等于没证。注意检查你的"理由"是不是其实就建立在结论之上。

### 坑 4：否定写错

- "$x > 0$" 的否定是 "$x \leq 0$"（不是 "$x < 0$"）
- "对所有 $x$ 都有 $P(x)$" 的否定是 "**存在某个** $x$ 使 $P(x)$ 不成立"

这一点在用**反证法**时极其重要。

> **例题**：下面这段"证明"踩了哪个坑？
> > 要证："如果一个数能被 6 整除，那么它能被 3 整除。"
> > 因为 12 能被 6 整除，12 也能被 3 整除，所以成立。
>
> **解**：踩了**坑 3 的近亲——以偏概全**。只验证 12 一个例子，并不能证明"所有"能被 6 整除的数。一个例子只能举反例（推翻命题），不能证明一般结论。

---

## 六、反证法的逻辑

反证法是初中开始接触的强大工具，它的逻辑骨架是：

> 要证 P。
> 假设 "非 P" 成立。
> 一步步推下去，导出矛盾（比如推出 "$0 > 1$"）。
> 所以"非 P" 不可能成立。
> 所以 P 成立。

为什么这样合理？因为命题非真即假：如果"非 P"导致荒谬，那 P 必须为真。

**写反证法时第一步必须写对"非 P"是什么**——这就是为什么前面强调"否定"。

> **例题**：用反证法证明"$\sqrt{2}$ 不是整数"。
> **证明**：
> 假设"非 P"成立，即 $\sqrt{2}$ **是**整数，设 $\sqrt{2} = n$（$n$ 为整数）。
> 那么 $n^2 = 2$。但 $1^2 = 1 < 2$，$2^2 = 4 > 2$，在 1 和 2 之间没有别的整数，
> 所以找不到整数 $n$ 使 $n^2 = 2$——矛盾！
> 所以假设不成立，$\sqrt{2}$ 不是整数。
>
> 注意第一步：P 是"$\sqrt{2}$ 不是整数"，它的否定"非 P"正是"$\sqrt{2}$ 是整数"。

---

## 七、写证明的自检清单

写完一个证明，请像查作业一样检查：

- [ ] 我用的每一个不等式/定理，**它的前提条件**满足了吗？
- [ ] 每一步是 "$\Rightarrow$" 还是 "$\iff$"？我有没有偷偷把单向当双向？
- [ ] 推理的**方向**对不对？是从已知推向结论，还是从结论倒推？
- [ ] 我有没有在某一步"省略"了一个其实需要论证的小命题？
- [ ] 结论是不是题目要求证的那个？（别证到一半证成了别的）

> **例题**：用清单检查下面这步"证明"。
> > 要证 $a + b \geq 2\sqrt{ab}$，已知 $a + b \geq 2\sqrt{ab}$，所以 $(\sqrt{a} - \sqrt{b})^2 \geq 0$，成立。
>
> **解**：对照第 3 条——推理**方向反了**：它从"要证的结论"出发去推已知，属于坑 1。正确做法是反过来，从 $(\sqrt{a}-\sqrt{b})^2 \geq 0$ 推向结论。

---

## 八、综合练习

下面按前面各章的顺序准备了一组练习。建议你边做边翻回对应章节，把"思维规则"用起来。**这里只给题目，不给答案——自己想通才算真懂。**

### A 组：命题（对应第一章）

A1. 判断下面各句是不是命题；是命题的，指出真假：
1. "0 是最小的自然数"
2. "把方程两边平方"
3. "任何三角形的内角和都是 180°"
4. "这道题好难啊！"
5. "$x + 1 = 5$"

A2. 给一个"开命题"，并说明给它的字母取什么值时为真、取什么值时为假。

### B 组：逻辑连接词（对应第二章）

B1. 写出下面各命题的**否定**：
1. "$x > 5$"
2. "$x \leq -1$"
3. "$a = 0$"
4. "这个班所有同学都及格了"

B2. 判断下面的"$\Rightarrow$"是否成立；若不成立，举一个反例：
1. $n$ 是 4 的倍数 $\Rightarrow n$ 是 2 的倍数
2. $n$ 是 2 的倍数 $\Rightarrow n$ 是 4 的倍数
3. $x^2 = 9 \Rightarrow x = 3$

B3. 把 "$|x| \leq 2$" 用"且"或"或"展开成两个不等式连接的形式。

### C 组：传递性与分离规则（对应第三章）

C1. 已知 $x > y$，$y > 7$，$7 > z$，用传递性写出 $x$ 与 $z$ 的大小关系，并说明每一步依据。

C2. 已知定理"两个奇数的乘积仍是奇数"。现在知道 3 和 9 都是奇数，请用分离规则断定一个结论。

### D 组：逻辑放大镜（对应第四章）

D1. 用"逻辑放大镜"分析下面这个证明，逐行回答：① 这一行是命题吗？② 它由哪一行/哪个定理推来？③ 用的是"$\Rightarrow$"还是"$\iff$"？

> **题**：设 $a > b > 0$，证明 $a^2 > b^2$。
>
> **证明**：
> 因为 $a > b$，所以 $a - b > 0$；
> 又因为 $a > 0, b > 0$，所以 $a + b > 0$；
> 所以 $(a-b)(a+b) > 0$；
> 即 $a^2 - b^2 > 0$；
> 所以 $a^2 > b^2$。

D2. 仿照 D1，给"若 $a > 0$，则 $a + \dfrac{1}{a} \geq 2$"写出证明，并标注每一步是"$\Rightarrow$"还是"$\iff$"。（提示：从 $\left(\sqrt{a} - \dfrac{1}{\sqrt{a}}\right)^2 \geq 0$ 出发。）

### E 组：找逻辑坑（对应第五章）

E1. 下面每段"证明"各踩了哪个坑（坑 1～坑 4）？请指出并说明理由：
1. 要证 $a^2 \geq 0$，因为 $a^2 \geq 0$，所以 $|a| \geq 0$，成立。
2. 因为 $\sqrt{ab} \leq \dfrac{a+b}{2}$，所以对任意实数 $a, b$ 都有 $a + b \geq 2\sqrt{ab}$。
3. "$x^2 > 0$"的否定写成"$x^2 < 0$"。

E2. 举一个"用一个例子就当作证明了一般结论"的错误例子，并说明它为什么不成立。

### F 组：反证法（对应第六章）

F1. 写出用反证法证明下列命题时，第一步的"非 P"分别是什么：
1. "$\sqrt{3}$ 不是整数"
2. "质数有无穷多个"
3. "若 $a^2$ 是偶数，则 $a$ 是偶数"

F2. 试着用反证法证明："如果 $a + b > 100$，那么 $a$、$b$ 中至少有一个大于 50。"（提示：先写对"非 P"。）

### G 组：自检清单（对应第七章）

G1. 任选你最近写过的一道证明题，逐条对照第七章的 5 条清单检查一遍，记录下你发现的问题。

---

## 结语

数学证明不是"凑出答案"，而是**一串严格的逻辑链条**：每一颗珠子（命题）都必须真，相邻两颗之间必须有一根可靠的丝线（推理规则）连着。

逻辑学就是这根"丝线"的研究。掌握了它，你写的不再是"看起来差不多的步骤"，而是**任何人都无法挑剔**的证明。
