# 常见不等式：均值不等式链


## 一、四种平均数与不等式链

对两个**正数** $a, b > 0$，定义四种平均数：

$$H = \frac{2ab}{a+b}, \quad G = \sqrt{ab}, \quad A = \frac{a+b}{2}, \quad Q = \sqrt{\frac{a^2+b^2}{2}}$$

分别称为**调和平均**、**几何平均**（等比中项）、**算术平均**（等差中项）和**平方平均**（均方根）。

举几个具体例子感受它们的大小：

| $a$ | $b$ | $H = \dfrac{2ab}{a+b}$ | $G = \sqrt{ab}$ | $A = \dfrac{a+b}{2}$ | $Q = \sqrt{\dfrac{a^2+b^2}{2}}$ |
|-----|-----|------------------------|-----------------|----------------------|--------------------------------|
| $1$ | $9$ | $1.8$ | $3$ | $5$ | $\sqrt{41} \approx 6.40$ |
| $4$ | $4$ | $4$ | $4$ | $4$ | $4$ |
| $2$ | $8$ | $3.2$ | $4$ | $5$ | $\sqrt{34} \approx 5.83$ |

四种平均数满足如下**不等式链**：

$$\boxed{H \leq G \leq A \leq Q}$$

等号当且仅当 $a = b$ 时同时成立。

本文分三节依次证明这三个不等式，并对每个不等式的常见变形和最值应用进行系统总结。

---

## 二、算术平均与几何平均：$A \geq G$

### 主定理

**定理**（算术平均 $\geq$ 几何平均）

对任意正数 $a, b > 0$，有

$$\frac{a+b}{2} \geq \sqrt{ab}$$

等号成立当且仅当 $a = b$。也可以写成：

$$a + b \geq 2\sqrt{ab}$$

**证明**：任何数的平方都 $\geq 0$。

$$(\sqrt{a} - \sqrt{b})^2 \geq 0$$

展开：

$$a - 2\sqrt{ab} + b \geq 0 \implies a + b \geq 2\sqrt{ab}$$

两边除以 $2$ 即得。等号成立当且仅当 $\sqrt{a} = \sqrt{b}$，即 $a = b$。$\blacksquare$

---

### 常见变形

**变形 1**：令 $a = x^2,\ b = y^2$

将 $a + b \geq 2\sqrt{ab}$ 中的 $a, b$ 换成 $x^2, y^2$（$x,y>0$）：

$$x^2 + y^2 \geq 2xy$$

**题目**：证明对任意正数 $x, y$，有 $x^2 + y^2 \geq 2xy$。

**证明**：

$$x^2 - 2xy + y^2 = (x-y)^2 \geq 0 \quad \checkmark$$

等号当且仅当 $x = y$。$\blacksquare$

> **口诀**：平方和 $\geq$ 两倍乘积。

---

**变形 2**：令 $b = \dfrac{1}{a}$

在 $a + b \geq 2\sqrt{ab}$ 中令 $b = \dfrac{1}{a}$（$a > 0$）：

$$a + \frac{1}{a} \geq 2\sqrt{a \cdot \frac{1}{a}} = 2$$

**题目**：证明对任意正数 $a$，有 $a + \dfrac{1}{a} \geq 2$。

**证明**：两边乘以 $a$：

$$a^2 + 1 \geq 2a \iff (a-1)^2 \geq 0 \quad \checkmark$$

等号当且仅当 $a = 1$。$\blacksquare$

---

**变形 3**：令 $a = \dfrac{x}{y},\ b = \dfrac{y}{x}$

在 $a + b \geq 2\sqrt{ab}$ 中令 $a = \dfrac{x}{y},\ b = \dfrac{y}{x}$（$x,y>0$）：

$$\frac{x}{y} + \frac{y}{x} \geq 2$$

**题目**：证明对任意正数 $x, y$，有 $\dfrac{x}{y} + \dfrac{y}{x} \geq 2$。

**证明**：两边乘以 $xy > 0$：

$$x^2 + y^2 \geq 2xy$$

即变形 1，已证。等号当且仅当 $x = y$。$\blacksquare$

---

**变形 4**：两边平方

**题目**：已知正数 $a, b$ 满足 $\sqrt{ab} \leq 3$，能说明 $ab \leq 9$ 吗？

**分析**：因为 $\sqrt{ab} > 0$，对不等式两边平方，方向不变：

$$ab = (\sqrt{ab})^2 \leq 9 \quad \checkmark$$

---

**变形 5**：两边取倒数

由 $\dfrac{a+b}{2} \geq \sqrt{ab}$，两边取倒数（两边均为正数，**方向改变**）：

$$\frac{2}{a+b} \leq \frac{1}{\sqrt{ab}}$$

两边乘以 $2ab > 0$，可得**调和平均 $\leq$ 几何平均**的预告：

$$\frac{2ab}{a+b} \leq \sqrt{ab}$$

**题目**：证明 $\dfrac{2ab}{a+b} \leq \sqrt{ab}$（$a,b>0$）。

**证明**：由 $a + b \geq 2\sqrt{ab} > 0$，两边取倒数，方向改变：

$$\frac{1}{a+b} \leq \frac{1}{2\sqrt{ab}}$$

两边乘以 $2ab > 0$：

$$\frac{2ab}{a+b} \leq \frac{2ab}{2\sqrt{ab}} = \sqrt{ab} \quad \checkmark$$

等号当且仅当 $a = b$。$\blacksquare$

> 这正是 $G \geq H$ 的内容，下一节将对它做更系统的研究。

---

**变形 6**：分母换元

**题目**：证明对正数 $a, b$，有 $\dfrac{a}{b} + \dfrac{b}{a} + 2 \geq \dfrac{(a+b)^2}{ab}$。

**证明**：右边展开：

$$\frac{(a+b)^2}{ab} = \frac{a^2+2ab+b^2}{ab} = \frac{a}{b} + 2 + \frac{b}{a}$$

两边相等，不等式为恒等式，自然成立。$\blacksquare$

> 这说明 $\dfrac{(a+b)^2}{ab} = \dfrac{a}{b}+\dfrac{b}{a}+2 \geq 2+2=4$，即 $(a+b)^2 \geq 4ab$，正是均值不等式两边平方。

---

**变形 7**：加同一个数后取倒数

**题目**：证明 $\dfrac{1}{1+a} + \dfrac{1}{1+b} \geq \dfrac{4}{2+a+b}$（$a,b>0$）。

**证明**：设 $x = 1+a,\ y = 1+b$，原式变为 $\dfrac{1}{x} + \dfrac{1}{y} \geq \dfrac{4}{x+y}$。

两边乘以 $xy(x+y) > 0$：

$$(x+y)^2 \geq 4xy \iff (x-y)^2 \geq 0 \quad \checkmark$$

等号当且仅当 $a = b$。$\blacksquare$

---

### 用 $A \geq G$ 求最值

**例题 1**：已知和，求积最大

$x + y = 10$，$x,y > 0$，求 $xy$ 的最大值。

**解**：由均值不等式：

$$\frac{x+y}{2} \geq \sqrt{xy} \implies 5 \geq \sqrt{xy} \implies xy \leq 25$$

等号当 $x = y = 5$ 时取到，最大值为 $\boxed{25}$。

---

**例题 2**：已知积，求和最小

$xy = 9$，$x,y > 0$，求 $x + y$ 的最小值。

**解**：由 $x + y \geq 2\sqrt{xy} = 2\sqrt{9} = 6$。

等号当 $x = y = 3$ 时取到，最小值为 $\boxed{6}$。

---

**例题 3**：求含倒数的式子的最小值

$x > 0$，求 $x + \dfrac{4}{x}$ 的最小值。

**解**：

$$x + \frac{4}{x} \geq 2\sqrt{x \cdot \frac{4}{x}} = 2\sqrt{4} = 4$$

等号当 $x = \dfrac{4}{x}$，即 $x = 2$ 时取到，最小值为 $\boxed{4}$。

---

**例题 4**：围矩形面积最大

用 20 米篱笆围矩形，设两边长为 $a$ 和 $b$，则 $a + b = 10$，$a,b > 0$。

**解**：由 $A \geq G$：

$$\frac{a+b}{2} \geq \sqrt{ab} \implies 5 \geq \sqrt{ab} \implies ab \leq 25$$

等号当 $a = b = 5$（正方形）时取到，最大面积为 $\boxed{25}$ 平方米。

---

## 三、几何平均与调和平均：$G \geq H$

### 主定理

**定理**（几何平均 $\geq$ 调和平均）

对任意正数 $a, b > 0$，有

$$\sqrt{ab} \geq \frac{2ab}{a+b}$$

等号成立当且仅当 $a = b$。

**证明**：因为 $\sqrt{ab} > 0$，$a + b > 0$，两边乘以 $(a+b)$：

$$\sqrt{ab}\,(a+b) \geq 2ab$$

两边再除以 $\sqrt{ab} > 0$：

$$a + b \geq 2\sqrt{ab}$$

这正是上一节证明的 $A \geq G$，成立。等号成立当且仅当 $a = b$。$\blacksquare$

> **关键联系**：$G \geq H$ 与 $A \geq G$ 互为等价，可以互推。

---

### 常见变形

**变形 1**：两边平方

由 $\sqrt{ab} \geq \dfrac{2ab}{a+b}$，两边均为正数，平方后方向不变，化简可得：

$$(a+b)^2 \geq 4ab$$

**题目**：证明对任意正数 $a, b$，有 $(a+b)^2 \geq 4ab$，并指出等号成立的条件。

**证明**：

$$(a+b)^2 - 4ab = a^2 - 2ab + b^2 = (a-b)^2 \geq 0 \quad \checkmark$$

等号当且仅当 $a = b$。$\blacksquare$

> 口诀：和的平方 $\geq$ 四倍积。

---

**变形 2**：两边取倒数（方向改变！）

由 $\sqrt{ab} \geq \dfrac{2ab}{a+b}$，取倒数后方向改变，注意 $\dfrac{a+b}{2ab} = \dfrac{1}{2}\!\left(\dfrac{1}{a}+\dfrac{1}{b}\right)$，故：

$$\frac{1}{a} + \frac{1}{b} \geq \frac{2}{\sqrt{ab}}$$

**题目**：证明对任意正数 $a, b$，有 $\dfrac{1}{a} + \dfrac{1}{b} \geq \dfrac{2}{\sqrt{ab}}$，并指出等号成立的条件。

**证明**：两边乘以 $\sqrt{ab} > 0$：

$$\sqrt{\frac{b}{a}} + \sqrt{\frac{a}{b}} \geq 2$$

令 $t = \sqrt{\dfrac{a}{b}} > 0$，即证 $t + \dfrac{1}{t} \geq 2$，即 $(t-1)^2 \geq 0$。$\checkmark$

等号当且仅当 $t = 1$，即 $a = b$。$\blacksquare$

---

**变形 3**：两边除以 $\sqrt{ab}$

由 $\sqrt{ab} \geq \dfrac{2ab}{a+b}$，两边除以 $\sqrt{ab} > 0$，得 $1 \geq \dfrac{2\sqrt{ab}}{a+b}$，即：

$$a + b \geq 2\sqrt{ab}$$

**结论**：$G \geq H$ 与 $A \geq G$ 是**等价**的，可以互推。

---

**变形 4**：令 $a = x^2,\ b = y^2$

将 $\sqrt{ab} \geq \dfrac{2ab}{a+b}$ 中的 $a, b$ 换成 $x^2, y^2$，化简可得：

$$x^2 + y^2 \geq 2xy$$

**题目**：证明对任意正数 $x, y$，有 $x^2 + y^2 \geq 2xy$，并指出等号成立的条件。

**证明**：$x^2 + y^2 - 2xy = (x-y)^2 \geq 0$。$\checkmark$ 等号当且仅当 $x = y$。$\blacksquare$

---

**变形 5**：两边乘以 $\dfrac{a+b}{ab}$

由 $\sqrt{ab} \geq \dfrac{2ab}{a+b}$，两边乘以 $\dfrac{a+b}{ab} > 0$，注意 $\dfrac{a+b}{\sqrt{ab}} = \sqrt{\dfrac{a}{b}} + \sqrt{\dfrac{b}{a}}$，得：

$$\sqrt{\frac{a}{b}} + \sqrt{\frac{b}{a}} \geq 2$$

**题目**：证明对任意正数 $a, b$，有 $\sqrt{\dfrac{a}{b}} + \sqrt{\dfrac{b}{a}} \geq 2$，并指出等号成立的条件。

**证明**：令 $t = \sqrt{\dfrac{a}{b}} > 0$，即证 $t + \dfrac{1}{t} \geq 2$，即 $t^2 - 2t + 1 = (t-1)^2 \geq 0$。$\checkmark$

等号当且仅当 $a = b$。$\blacksquare$

---

**变形 6**：$H$ 的改写

$$H = \frac{2ab}{a+b} = \frac{2}{\dfrac{1}{a}+\dfrac{1}{b}}$$

由变形 2 知 $\dfrac{1}{a}+\dfrac{1}{b} \geq \dfrac{2}{\sqrt{ab}}$，取倒数（方向改变）：

$$H = \frac{2}{\dfrac{1}{a}+\dfrac{1}{b}} \leq \frac{2}{\dfrac{2}{\sqrt{ab}}} = \sqrt{ab} = G$$

与主定理一致。

**题目**：已知 $\dfrac{1}{a}+\dfrac{1}{b} = 1$（$a,b>0$），证明 $\sqrt{ab} \geq 2$，并指出等号成立时 $a, b$ 各为多少。

**证明**：由变形 2：$\dfrac{1}{a}+\dfrac{1}{b} \geq \dfrac{2}{\sqrt{ab}}$，代入条件：

$$1 \geq \frac{2}{\sqrt{ab}} \implies \sqrt{ab} \geq 2 \quad \checkmark$$

等号当且仅当 $a = b$，代回条件得 $a = b = 2$。$\blacksquare$

---

### 用 $G \geq H$ 求最值

**例题 1**：已知几何平均，求调和平均的上界

$a, b > 0$，$\sqrt{ab} = 6$，求 $\dfrac{2ab}{a+b}$ 的最大值。

**解**：由 $G \geq H$：

$$\frac{2ab}{a+b} \leq \sqrt{ab} = 6$$

等号当 $a = b = 6$ 时取到，最大值为 $\boxed{6}$。

---

**例题 2**：分式的最小值

$a > 0$，求 $\dfrac{a^2+1}{a}$ 的最小值。

**解**：

$$\frac{a^2+1}{a} = a + \frac{1}{a} \geq 2$$

等号当 $a = 1$ 时取到，最小值为 $\boxed{2}$。

---

**例题 3**：和固定时，调和平均的最大值

$a + b = 10$，$a, b > 0$，求 $\dfrac{2ab}{a+b}$ 的最大值。

**解**：

$$H = \frac{2ab}{a+b} = \frac{2ab}{10} = \frac{ab}{5}$$

由变形 1：$(a+b)^2 \geq 4ab$，故 $ab \leq 25$，从而 $H \leq 5$。

等号当 $a = b = 5$ 时取到，最大值为 $\boxed{5}$。

---

**例题 4**：速度问题中的调和平均

小明去学校速度为 $a$ 米/分钟，返回速度为 $b$ 米/分钟，设距离为 $d$ 米。

$$\text{往返平均速度} = \frac{2d}{\dfrac{d}{a}+\dfrac{d}{b}} = \frac{2ab}{a+b} = H$$

由 $G \geq H$：平均速度 $H \leq \sqrt{ab} = G$，等号当且仅当 $a = b$。$\blacksquare$

---

## 四、平方平均与算术平均：$Q \geq A$

### 主定理

**定理**（平方平均 $\geq$ 算术平均）

对任意正数 $a, b > 0$，有

$$\sqrt{\frac{a^2+b^2}{2}} \geq \frac{a+b}{2}$$

等号成立当且仅当 $a = b$。

**证明**：两边均为正数，平方后不等号方向不变：

$$\frac{a^2+b^2}{2} \geq \frac{(a+b)^2}{4}$$

两边乘以 $4$，展开整理：

$$2a^2+2b^2 \geq a^2+2ab+b^2 \iff (a-b)^2 \geq 0 \quad \checkmark$$

等号成立当且仅当 $a = b$。$\blacksquare$

---

### 常见变形

**变形 1**：展开形式

由 $Q \geq A$ 两边平方，得等价不等式：

$$2(a^2+b^2) \geq (a+b)^2$$

**题目**：证明对任意正数 $a, b$，有 $2(a^2+b^2) \geq (a+b)^2$，并指出等号成立的条件。

**证明**：

$$2(a^2+b^2) - (a+b)^2 = a^2-2ab+b^2 = (a-b)^2 \geq 0 \quad \checkmark$$

等号当且仅当 $a = b$。$\blacksquare$

> **与 $A \geq G$ 的联系**：移项得 $a^2+b^2 \geq 2ab$，正是第二节变形 1 的结论，说明 $Q \geq A$ 蕴含了 $A \geq G$，是更强的不等式。

---

**变形 2**：已知和，推出平方和的下界

由变形 1 两边除以 $2$：

$$a^2+b^2 \geq \frac{(a+b)^2}{2}$$

**题目**：已知正数 $a, b$ 满足 $a+b = 6$，证明 $a^2+b^2 \geq 18$，并指出等号成立的条件。

**证明**：

$$a^2+b^2 \geq \frac{(a+b)^2}{2} = \frac{36}{2} = 18 \quad \checkmark$$

等号当且仅当 $a = b = 3$。$\blacksquare$

> 口诀：**和固定时，平方和最小在两数相等**。

---

**变形 3**：已知平方和，推出和的上界

由 $(a+b)^2 \leq 2(a^2+b^2)$，两边取平方根：

$$a+b \leq \sqrt{2(a^2+b^2)}$$

**题目**：已知正数 $a, b$ 满足 $a^2+b^2 = 18$，证明 $a+b \leq 6$，并指出等号成立的条件。

**证明**：$(a+b)^2 \leq 2(a^2+b^2) = 36$，故 $a+b \leq 6$。$\checkmark$

等号当且仅当 $a = b = 3$。$\blacksquare$

> 口诀：**平方和固定时，和最大在两数相等**。

---

**变形 4**：令 $b = ka$（比例代换）

在 $2(a^2+b^2) \geq (a+b)^2$ 中令 $b = ka$（$k > 0$），两边除以 $a^2 > 0$：

$$2(1+k^2) \geq (1+k)^2$$

**题目**：证明对任意正数 $k$，有 $2(1+k^2) \geq (1+k)^2$，并指出等号成立的条件。

**证明**：

$$2(1+k^2) - (1+k)^2 = k^2-2k+1 = (k-1)^2 \geq 0 \quad \checkmark$$

等号当且仅当 $k = 1$，即 $b = a$。$\blacksquare$

> 这说明 $Q \geq A$ 的本质只依赖于两数的**比值**，与具体数值大小无关。

---

**变形 5**：完整不等式链的验证

综合前两节的结论，四种平均数满足：

$$\frac{2ab}{a+b} \leq \sqrt{ab} \leq \frac{a+b}{2} \leq \sqrt{\frac{a^2+b^2}{2}}$$

**题目**：已知 $a = 1, b = 9$，计算 $H, G, A, Q$，验证不等式链成立。

**解**：

$$H = \frac{18}{10} = 1.8, \quad G = \sqrt{9} = 3, \quad A = \frac{10}{2} = 5, \quad Q = \sqrt{\frac{1+81}{2}} = \sqrt{41} \approx 6.40$$

确实 $1.8 < 3 < 5 < 6.40$。$\blacksquare$

---

**变形 6**：直接比较 $Q$ 与 $G$

由变形 1，$2(a^2+b^2) \geq (a+b)^2 \geq 4ab$，故 $a^2+b^2 \geq 2ab$，即 $Q \geq G$。

**题目**：证明 $\dfrac{a^2+b^2}{2} \geq ab$（即 $Q^2 \geq G^2$，从而 $Q \geq G$），并指出等号成立的条件。

**证明**：

$$\frac{a^2+b^2}{2} - ab = \frac{(a-b)^2}{2} \geq 0 \quad \checkmark$$

等号当且仅当 $a = b$。$\blacksquare$

> 这给出了 $Q \geq G$ 的直接证明，无需经过 $A$。

---

### 用 $Q \geq A$ 求最值

**例题 1**：已知平方和，求和的最大值

$a^2+b^2 = 50$，$a, b > 0$，求 $a+b$ 的最大值。

**解**：由变形 3：

$$(a+b)^2 \leq 2(a^2+b^2) = 100$$

故 $a+b \leq 10$。等号当 $a = b = 5$ 时取到，最大值为 $\boxed{10}$。

---

**例题 2**：已知和，求平方和的最小值

$a+b = 8$，$a, b > 0$，求 $a^2+b^2$ 的最小值。

**解**：由变形 2：

$$a^2+b^2 \geq \frac{(a+b)^2}{2} = \frac{64}{2} = 32$$

等号当 $a = b = 4$ 时取到，最小值为 $\boxed{32}$。

---

**例题 3**：直接求 $Q$ 的最小值

$a+b = 10$，$a, b > 0$，求 $Q = \sqrt{\dfrac{a^2+b^2}{2}}$ 的最小值。

**解**：由 $Q \geq A = \dfrac{a+b}{2} = 5$，等号当 $a = b = 5$ 时取到，最小值为 $\boxed{5}$。

---

**例题 4**：矩形的面积与对角线

用 20 米篱笆围一个矩形，设两边长为 $a$ 和 $b$，则 $a+b = 10$，$a, b > 0$。

**第 1 问**：求面积 $S = ab$ 的最大值。（用 $A \geq G$）

由 $A \geq G$：$\dfrac{a+b}{2} \geq \sqrt{ab}$，即 $5 \geq \sqrt{ab}$，故 $ab \leq 25$。

等号当 $a = b = 5$（正方形）时取到，最大面积为 $25$ 平方米。

**第 2 问**：求对角线长度 $d = \sqrt{a^2+b^2}$ 的最小值。（用 $Q \geq A$）

由变形 2：$a^2+b^2 \geq \dfrac{(a+b)^2}{2} = 50$，故 $d \geq \sqrt{50} = 5\sqrt{2}$。

等号当 $a = b = 5$ 时取到，最短对角线为 $5\sqrt{2} \approx 7.07$ 米。

> **有趣的发现**：面积最大和对角线最短，同时在正方形时取到！

---

## 五、常见错误提醒

| 错误做法 | 正确理解 |
|----------|----------|
| 取倒数后方向不变 | 正数取倒数，不等号**方向改变** |
| 平方时不检查两边是否非负 | 只有两边都**非负**时，平方才保持不等号方向 |
| $\sqrt{ab} = \sqrt{a}\cdot\sqrt{b}$ 用于负数 | 只对**非负数**成立 |
| 将 $Q \geq A$ 与 $A \geq G$ 弄反 | $Q \geq A \geq G \geq H$，$Q$ 是四种平均中**最大**的 |
| 把 $Q$ 误写为 $\sqrt{a^2+b^2}$ | 注意 $Q = \sqrt{\dfrac{a^2+b^2}{2}}$，分母是 $\mathbf{2}$，别漏掉 |
| 忘记验证等号能取到 | 求最值时必须验证等号成立的条件 |
| 将 $H = \frac{2ab}{a+b}$ 化简时出错 | 分子分母不能随意约分，需整体处理 |

---

## 六、总结

$$\frac{2ab}{a+b} \leq \sqrt{ab} \leq \frac{a+b}{2} \leq \sqrt{\frac{a^2+b^2}{2}}$$

等号当且仅当 $a = b$ 时全部同时成立。

**$A \geq G$ 的常用变形**：

| 变形 | 式子 | 等号条件 |
|------|------|----------|
| 原式 | $\dfrac{a+b}{2} \geq \sqrt{ab}$ | $a=b$ |
| 换 $x^2,y^2$ | $x^2+y^2 \geq 2xy$ | $x=y$ |
| 令 $b=1/a$ | $a+\dfrac{1}{a} \geq 2$ | $a=1$ |
| 令 $a=x/y$ | $\dfrac{x}{y}+\dfrac{y}{x} \geq 2$ | $x=y$ |
| 两边平方 | $(a+b)^2 \geq 4ab$ | $a=b$ |
| 取倒数 | $\dfrac{2ab}{a+b} \leq \sqrt{ab}$（预告 $G \geq H$） | $a=b$ |

**$G \geq H$ 的常用变形**：

| 变形 | 式子 | 等号条件 |
|------|------|----------|
| 两边平方 | $(a+b)^2 \geq 4ab$ | $a=b$ |
| 取倒数 | $\dfrac{1}{a}+\dfrac{1}{b} \geq \dfrac{2}{\sqrt{ab}}$ | $a=b$ |
| 等价式 | $a+b \geq 2\sqrt{ab}$（即 $A \geq G$） | $a=b$ |
| 乘 $\frac{a+b}{ab}$ | $\sqrt{\dfrac{a}{b}}+\sqrt{\dfrac{b}{a}} \geq 2$ | $a=b$ |

**$Q \geq A$ 的常用变形**：

| 变形 | 式子 | 等号条件 |
|------|------|----------|
| 原式 | $\sqrt{\dfrac{a^2+b^2}{2}} \geq \dfrac{a+b}{2}$ | $a=b$ |
| 两边平方 | $2(a^2+b^2) \geq (a+b)^2$ | $a=b$ |
| 已知和 $s$ | $a^2+b^2 \geq \dfrac{s^2}{2}$（平方和下界） | $a=b$ |
| 已知平方和 $k$ | $a+b \leq \sqrt{2k}$（和的上界） | $a=b$ |
| 比例代换 | $2(1+k^2) \geq (1+k)^2$ | $k=1$ |
| 推论 | $\dfrac{a^2+b^2}{2} \geq ab$（即 $Q \geq G$） | $a=b$ |

---

## 七、练习题

### 第一部分：$A \geq G$ 的变形

**A1.** 已知正数 $x, y$ 满足 $x+y = 6$，求 $xy$ 的最大值，并指出取到最大值时 $x, y$ 各为多少。

**A2.** 已知正数 $x, y$ 满足 $x+y = 4$，求 $xy$ 的最大值，并指出取到最大值时 $x, y$ 各为多少。

**A3.** 已知正数 $a, b$ 满足 $a+b = 1$，求 $ab$ 的最大值，并指出取到最大值时 $a, b$ 各为多少。

**B1.** 已知正数 $x, y$ 满足 $xy = 4$，求 $x+y$ 的最小值，并指出取到最小值时 $x, y$ 各为多少。

**B2.** 已知正数 $x, y$ 满足 $xy = 25$，求 $x+y$ 的最小值，并指出取到最小值时 $x, y$ 各为多少。

**B3.** 已知正数 $a, b$ 满足 $ab = 3$，求 $2a + 3b$ 的最小值，并指出取到最小值时 $a, b$ 各为多少。（提示：对 $2a$ 和 $3b$ 用均值不等式）

**C1.** 已知 $a > 0$，求 $a + \dfrac{9}{a}$ 的最小值，并指出取到最小值时 $a$ 的值。

**C2.** 已知 $a > 0$，求 $2a + \dfrac{8}{a}$ 的最小值，并指出取到最小值时 $a$ 的值。

**C3.** 已知 $a > 0$，求 $a + \dfrac{1}{4a}$ 的最小值，并指出取到最小值时 $a$ 的值。

**D1.** 用 $24$ 米篱笆围一个矩形菜地，设两边长为 $a$ 和 $b$（单位：米），$a, b > 0$。求面积 $ab$ 的最大值，并说明此时菜地的形状。

**D2.** 将正数 $N$ 分成两个正数 $a$ 和 $b$（即 $a + b = N$，$a, b > 0$）。
1. 证明 $ab \leq \dfrac{N^2}{4}$，并指出等号成立的条件。
2. 将 $N = 10$ 分成两个正整数之和，哪种分法使乘积最大？

---

### 第二部分：$G \geq H$ 的变形

**A1.** 已知正数 $a, b$ 满足 $ab = 4$，求 $\dfrac{2ab}{a+b}$ 的最大值，并指出取到最大值时 $a, b$ 各为多少。

**A2.** 已知正数 $a, b$ 满足 $\sqrt{ab} = 5$，求 $\dfrac{2ab}{a+b}$ 的最大值，并指出取到最大值时 $a, b$ 各为多少。

**A3.** 已知正数 $a, b$ 满足 $\dfrac{1}{a}+\dfrac{1}{b} = 2$，求 $\sqrt{ab}$ 的最小值，并指出等号成立时 $a, b$ 各为多少。

**B1.** 已知 $a > 0$，求 $\dfrac{a^2 + 4}{a}$ 的最小值，并指出取到最小值时 $a$ 的值。

**B2.** 已知 $a > 0$，求 $\dfrac{a^2 + 9}{a}$ 的最小值，并指出取到最小值时 $a$ 的值。

**B3.** 已知 $a > 0$，求 $4a + \dfrac{1}{a}$ 的最小值，并指出取到最小值时 $a$ 的值。

**C1.** 已知正数 $a, b$ 满足 $a + b = 6$，求 $\dfrac{2ab}{a+b}$ 的最大值，并指出取到最大值时 $a, b$ 各为多少。

**C2.** 已知正数 $a, b$ 满足 $a + b = 4$，求 $ab$ 的最大值以及 $\dfrac{2ab}{a+b}$ 的最大值。

**D1.** 一辆汽车从 $A$ 城到 $B$ 城，上坡速度为 $u$ 千米/时，下坡速度为 $v$ 千米/时（$u < v$），上下坡路程各为 $d$ 千米。
1. 写出全程平均速度的表达式。
2. 证明全程平均速度不超过 $\sqrt{uv}$。

**D2.** 小李完成一项工作，前半段时间效率为每小时 $a$ 件，后半段时间效率为每小时 $b$ 件（$a \neq b$）。
1. 写出全程平均效率的表达式。
2. 判断全程平均效率与 $\dfrac{a+b}{2}$ 的大小关系，并给出理由。

---

### 第三部分：$Q \geq A$ 的变形

**A1.** 已知正数 $a, b$ 满足 $a^2+b^2 = 8$，求 $a+b$ 的最大值，并指出取到最大值时 $a, b$ 各为多少。

**A2.** 已知正数 $a, b$ 满足 $a^2+b^2 = 72$，求 $a+b$ 的最大值，并指出取到最大值时 $a, b$ 各为多少。

**A3.** 已知正数 $a, b$ 满足 $a^2+b^2 = 2$，求 $Q = \sqrt{\dfrac{a^2+b^2}{2}}$ 的值，再求 $a+b$ 的最大值，并验证 $Q \geq A$。

**B1.** 已知正数 $a, b$ 满足 $a+b = 4$，求 $a^2+b^2$ 的最小值，并指出取到最小值时 $a, b$ 各为多少。

**B2.** 已知正数 $a, b$ 满足 $a+b = 10$，求 $a^2+b^2$ 的最小值。

**B3.** 已知正数 $a, b$ 满足 $a+b = 6$，分别求 $a^2+b^2$ 的最小值以及 $Q = \sqrt{\dfrac{a^2+b^2}{2}}$ 的最小值。

**C1.** 已知正数 $a, b$ 满足 $a^2+b^2 = 50$，求 $ab$ 的最大值。（提示：$(a+b)^2 = a^2+2ab+b^2$，先求 $a+b$ 的最大值）

**C2.** 设 $a+b = 6$，$a, b > 0$，计算或估计 $H, G, A, Q$ 各自的范围，并用不等式链表示它们的大小关系（等号条件写清楚）。

**C3.** 证明：对任意正数 $a, b$，有 $\sqrt{\dfrac{a^2+b^2}{2}} \geq \sqrt{ab}$（即 $Q \geq G$），并说明这可以由 $Q \geq A \geq G$ 推出，也可以直接证明。

**D1.** 矩形的周长为 $2s$（即 $a+b = s$，$a, b > 0$）。
1. 求面积 $ab$ 的最大值。
2. 求对角线 $\sqrt{a^2+b^2}$ 的最小值，并说明取最小值时矩形是正方形。

**D2.** 小明用两段绳子，第一段长 $a$ 分米，第二段长 $b$ 分米，满足 $a^2+b^2 = 200$。
1. 求两段绳子总长 $a+b$ 的最大值。
2. 若将两段绳子分别围成正方形，两个正方形的面积之和是多少？面积之和与总长的关系符合哪个不等式？

---

### 第四部分：综合题

> 以下各题需综合运用 $A \geq G$、$G \geq H$、$Q \geq A$ 中的两个或全部结论。

**E1.** 已知 $a = 3, b = 12$，先求出 $a+b, ab, a^2+b^2$，再计算 $H, G, A, Q$ 的精确值，验证不等式链 $H \leq G \leq A \leq Q$，并求 $Q - H$ 的值。

**E2.** 已知正数 $a, b$ 满足 $a+b = 10$，$ab = 16$，不解方程，直接计算 $H, G, A, Q$（$Q$ 保留根号形式），并验证不等式链。

（提示：$a^2+b^2 = (a+b)^2 - 2ab$）

**F1.** 已知正数 $a, b$ 满足 $a^2+b^2 = 2$，依次证明：
1. $a + b \leq 2$；
2. $ab \leq 1$（提示：利用 $(a+b)^2 = a^2+2ab+b^2$）；
3. $H = \dfrac{2ab}{a+b} \leq 1$（提示：先由第 2 步得 $G \leq 1$，再用 $H \leq G$）。

并分别指出各步等号成立时 $a, b$ 的值。

**F2.** 证明：对任意正数 $a, b$，有

$$\frac{1}{a} + \frac{1}{b} \geq \frac{4}{a+b}$$

并指出等号成立的条件。（提示：注意 $\dfrac{1}{a}+\dfrac{1}{b} = \dfrac{a+b}{ab}$，再利用 $(a+b)^2 \geq 4ab$）

**F3.** 已知正数 $a, b$ 满足 $a + b = 1$，用不等式链的结论依次证明下列三个不等式，并在每步后说明用的是哪个不等式：
1. $ab \leq \dfrac{1}{4}$；
2. $a^2+b^2 \geq \dfrac{1}{2}$；
3. $\dfrac{1}{a}+\dfrac{1}{b} \geq 4$。

**G1.** 已知 $a > 0$，求 $\left(a + \dfrac{1}{a}\right)^2$ 的最小值，并指出取到最小值时 $a$ 的值。

**G2.** 已知正数 $x$ 满足 $x + \dfrac{1}{x} = 3$：
1. 求 $x^2 + \dfrac{1}{x^2}$ 的值。（提示：$\left(x+\dfrac{1}{x}\right)^2 = x^2 + 2 + \dfrac{1}{x^2}$）
2. 以 $x$ 和 $\dfrac{1}{x}$ 作为两个正数，计算它们的平方平均 $Q = \sqrt{\dfrac{x^2+\frac{1}{x^2}}{2}}$ 和算术平均 $A = \dfrac{x+\frac{1}{x}}{2}$，验证 $Q \geq A$。

**G3.** 已知正数 $a, b$ 的算术平均 $A = 5$，调和平均 $H = 3$。
1. 由 $A$ 求 $a+b$；由 $H$ 和 $a+b$ 求 $ab$；进而求 $G = \sqrt{ab}$（化简为最简根式）。
2. 由 $a+b$ 和 $ab$ 求 $a^2+b^2$；进而求 $Q = \sqrt{\dfrac{a^2+b^2}{2}}$（化简为最简根式）。
3. 将 $H, G, A, Q$ 的值代入不等式链，写出具体数值的不等式，验证其成立。
